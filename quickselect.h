#ifndef QUICKSELECT
#define QUICKSELECT

#include <stdio.h>
#include <stdlib.h>

double qselect(double *v, int len, int k);

/*
void switcharoo(double *array, int index1, int index2);

int partition(double *array, int left, int right, int pivotIndex);

double qselect(double *array, int left, int right, int n);

double quickselect(double *array, int array_size, int k);
*/

#endif
